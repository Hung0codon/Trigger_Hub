apiVersion: v1
kind: Service
metadata:
  name: tf1-platform-service
  labels:
    app: tf1-platform-service
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: tf1-platform-service
  type: ClusterIP
