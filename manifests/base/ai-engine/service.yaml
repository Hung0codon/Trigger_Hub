apiVersion: v1
kind: Service
metadata:
  name: tf1-ai-triage-engine
  labels:
    app: tf1-ai-triage-engine
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: tf1-ai-triage-engine
  type: ClusterIP

